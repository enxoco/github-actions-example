# .github/workflows/workflow-2.yml
name: Workflow 2

on:
  pull_request:
    types: [labeled, unlabeled]

jobs:
  check-pr-label:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: .github/workflows/workflow-1.yml
        name: Check if Workflow 1 passed
        if: contains(github.event.pull_request.labels.*.name, 'build test environment')
        run: |
          echo "Running"
