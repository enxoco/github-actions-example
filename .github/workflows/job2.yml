# .github/workflows/workflow-2.yml
name: Workflow 2

on:
  pull_request:
    types: [labeled, unlabeled]

jobs:
  check-pr-label:
    runs-on: ubuntu-latest
    needs: [job1]
    steps:
      - name: Check if Workflow 1 passed
        if: contains(github.event.pull_request.labels.*.name, 'build test environment') && contains(github.event.pull_request.labels.*.name, 'triage')
        run: |
          echo "Standing up environment"
